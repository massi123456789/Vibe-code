{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -d .claude/skills/ui-ux-pro-max ] || [ -d \"$HOME/.claude/skills/ui-ux-pro-max\" ] || { command -v uipro >/dev/null 2>&1 || npm install -g ui-ux-pro-max-cli >/dev/null 2>&1; uipro init --ai claude --global >/dev/null 2>&1; }; true"
          }
        ]
      }
    ]
  }
}
